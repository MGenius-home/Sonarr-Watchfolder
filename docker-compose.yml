version: "3"
services:
  sonarr:
    build: .
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - ./config:/config
      - ./tv:/tv
      - ./watch:/watch
    ports:
      - 8989:8989
    restart: unless-stopped
